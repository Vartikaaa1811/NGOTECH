import React from 'react'

const page = () => {
  return (
    <div>
      this page will have a form of fixed amount donations for the ngo welfare. + it will also have a option in which people will put there mail/input if they have goods to donate, and can communicate, 
      like they fill fill the form, amd admin can see it and will contact them manually,
       not sure if this feature will be by mail or form.
    </div>
  )
}

export default page
