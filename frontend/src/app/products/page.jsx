import React from "react";

const page = () => {
  return (
    <div>
      this page/section will have all the products created by ngo people.
      and will nead a form with details, and image of the product, like first nead a form,
       and when submit the form, it will show in this product page, followed by cart and then checkout,
        mail confirmation, and razorpay at last.

    </div>
  );
};

export default page;
